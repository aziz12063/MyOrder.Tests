using Fluxor;

namespace MyOrder.Store.Configuration
{
    public static class ActionMappingConfiguration
    {
        //public static List<ActionMapping> Mappings = new List<ActionMapping>();

        //public static void RegisterMapping<TAction>(Action<object, TAction> reducer, Func<object, TAction,IDispatcher , Task> effect = null)
        //{
        //    Mappings.Add(new ActionMapping(
        //        typeof(TAction),
        //        (state, action) => reducer(state, (TAction)action),
        //        (state, action, dispatcher) => effect(state, (TAction)action, dispatcher )
        //    ));
        //}
    }
}
